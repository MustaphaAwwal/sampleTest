version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - cd frontend
      - npm install
      - echo "$PWD"
  build:
    commands:
      - npm run build
      - cd ..
      - echo "$PWD"
      - ls frontend/build

